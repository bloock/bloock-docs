---
title: 'Prepare data'
sidebar_position: 1
---

# Prepare data

The first step of the certification process is to prepare the data that you want to certify through BLOOCK.

This basically translates to load this data to the SDK, standardize it and compute a hash using the algorithm that BLOOCK uses internally (Keccak256). Luckily, all this process is done by the SDK internally so all you need to do is&#x20;

## Examples

import SdkCodeBlock from '/docs/partials/_sdk-code-block.mdx';

import PrepareRecordJs from '!!raw-loader!@site/examples/record/prepare_record/prepare_record.ts';
import PrepareRecordPy from '!!raw-loader!@site/examples/record/prepare_record/prepare_record.py';
import PrepareRecordJava from '!!raw-loader!@site/examples/record/prepare_record/PrepareRecord.java';
import PrepareRecordPhp from '!!raw-loader!@site/examples/record/prepare_record/prepare_record.php';
import PrepareRecordGo from '!!raw-loader!@site/examples/record/prepare_record/prepare_record.go';

<SdkCodeBlock
  ts={PrepareRecordJs}
  py={PrepareRecordPy}
  java={PrepareRecordJava}
  php={PrepareRecordPhp}
  go={PrepareRecordGo}
/>
