import { PRODUCTS } from '@site/src/products';

<CardSection
  id="Quickstart"
  title="Quickstart"
  description="Learn how to integrate BLOOCK into your product."
>
  {PRODUCTS.map((product) => {
    return (
      <Card
        title={product.title}
        icon={<product.icon />}
        to={product.link}
        description={product.text}
      />
    );
  })}
</CardSection>
