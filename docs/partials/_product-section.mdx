import { PRODUCTS } from '@site/src/products';

<CardSection id="Quickstart">
  {PRODUCTS.map((product) => {
    return (
      <Card
        title={product.title}
        icon={<product.icon />}
        to={product.link}
        description={product.text}
      />
    );
  })}
</CardSection>
